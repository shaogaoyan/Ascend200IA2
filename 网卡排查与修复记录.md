# Atlas 200I DK A2 网卡排查与修复记录

> 板卡：Atlas 200I DK A2（hostname `davinci-mini`，Hi1910B SoC + 昇腾 310B NPU）
> 排查时间：2026-08-17
> 结果：**异常系统网卡已修复**，可 ping 可 SSH（192.168.137.100，root/Mind@123）

---

## 1. 背景

板子有两套系统：

| 系统 | 内核 | rootfs | 网卡初始状态 |
|---|---|---|---|
| 正常系统 | `5.10.0+`（官方）→ 后升级到 `6.6.0-72.0.0.76.h914.eulerosv2r15.ascend.aarch64` | Ubuntu 22.04（带昇腾 davinci 驱动） | ✅ 正常（eth0/eth1/usb0） |
| 异常系统 | `6.6.0-72.0.0.76.h914.eulerosv2r15.ascend.aarch64` | Ubuntu 22.04（精简，无 davinci 驱动） | ❌ 只有 lo，无网卡 |

最初症状：**异常系统只有 lo，eth0/eth1 完全没生成**。

---

## 2. 排查过程与关键证据

### 2.1 第一轮：设备树对比 —— 设备树不是问题

从 `/sys/firmware/fdt` 导出两套设备树并反编译（宿主机 `dtc -I dtb -O dts`）：

- `atlas200dk-fdt-5.10.dtb`（正常，52,436 B）→ `normal.dts`
- `atlas200dk-fdt-abnormal.dtb`（异常，56,103 B）→ `abnormal.dts`

**结论：两棵树根节点、网卡节点、USB 节点逐字节完全相同。**

```
# 正常/异常一致（关键节点）
compatible = "hisilicon,hi1910B-evb", "hisilicon,ascend610";
xge0@a7100000 { compatible = "hisilicon,hns3-platform-device"; status = "ok"; ... }
hiusbc1@A5080000 { compatible = "hisilicon,hiusbc"; ... }
chosen { bootargs = "console=ttyAMA0,115200 ..."; }
```

差异仅在昇腾 NPU 周边（`resource_manage`、`tsdrv`、`vo` 等 38 个节点），与网卡无关。
**→ 网卡不出，不是设备树的问题。**

### 2.2 第二轮：根因定位 —— rootfs 缺 davinci 驱动包，不是内核版本

用户做了一个关键对照实验：**把正常系统也升级到同款 6.6 内核，网卡依然正常。**

对比证据（同款 `6.6.0-72...eulerosv2r15.ascend` 内核）：

| 对比项 | 正常系统（网卡✅） | 异常系统（网卡❌） |
|---|---|---|
| `/proc/modules` 里 hclgeplf | ✅ `hclgeplf` 已加载 | ❌ 无 |
| `hclgeplf.ko` 文件位置 | ✅ `/var/davinci/driver/hclgeplf.ko` | ❌ 不存在 |
| `/var/davinci/driver/` | ✅ **165 个 .ko**（昇腾全套驱动） | ❌ 目录整个没有 |
| xge0/xge1 绑定驱动 | ✅ `hns3-platform` | ❌ 空 |
| 驱动形态 | out-of-tree 模块（`(OE)` 标记） | — |

**根因**：`hclgeplf.ko`（HNS3 **平台**硬件层驱动）是昇腾 davinci 驱动包里的**独立模块**，由
`start-davinci.service → /var/minirc_boot.sh → /var/davinci/scripts/minirc_sys_init.sh`
按 `hnae3.ko → hns3.ko → hclgeplf.ko` 顺序 insmod。正常系统装了完整 davinci 包，异常系统是精简 rootfs 没装。

**与 NPU 缺失是同一个根因**（`/var/Ascend/` 下连 `.run` 驱动包都没有）。

### 2.3 修复网卡生成：解压 davinci 驱动包

从正常系统打包 `/var/davinci/` 到宿主机 → `davinci.tar.gz`（32M，215 文件 / 165 个 .ko）。
导入异常系统 `/var/` 后，执行 `minirc_sys_init.sh` 加载驱动，**eth0/eth1 成功生成**。

dmesg 确认驱动加载成功：

```
hns3-platform a7100000.xge0: Start to platform probe
hns3-platform a7100000.xge0: hclge_plf id:0, ... num_tqps:8
hns3-platform a7100000.xge0: hns3_platform_probe success.
```

### 2.4 链路不通（NO-CARRIER）→ 缺 PHY 驱动 broadcom

网卡生成了，但 `ethtool` 显示两个口都 `Link detected: no`（NO-CARRIER）。

关键证据：
- dmesg：`PHY_ID : 0x11a`，PHY attach 成功，但 **irq=POLL**
- `/lib/modules/.../drivers/net/phy/` 有 `broadcom.ko`、`bcm-phy-lib.ko`，**但没加载**
- `readlink /sys/bus/mdio_bus/devices/mii-*.xge*:0X/driver` → `Generic PHY`（不是 broadcom）

**根因**：PHY 芯片是 Broadcom（`0x11a`），`hclgeplf` insmod 时被 Generic PHY 接管，载波检测不工作。
**修复**：`modprobe broadcom` → eth1 `1000Mb/s, Link detected: yes`（Full duplex）。

### 2.5 ping 不通 → eth0 冲突 IP + rp_filter 严格模式

链路 up 了，但 ping 双向不通。

关键证据：
- eth1 RX 计数在涨、TX 几乎不动（回包想走别的接口发不出去）
- 板子上有**两个冲突直连路由**：
  ```
  192.168.137.0/24 dev eth0 src 192.168.137.88 linkdown   ← eth0 手动配的幽灵 IP
  192.168.137.0/24 dev eth1 src 192.168.137.100           ← 网线实际所在
  ```
- `rp_filter = 2`（strict 严格反向路径过滤）
- 宿主机 tcpdump：只有 echo request 发出，无 reply 回来

**根因**：eth0 上被手动配了 `192.168.137.88/24`（与 eth1 同网段冲突），回包路由选择错误 + rp_filter 严格模式拦截。
**修复**：`ip addr del 192.168.137.88/24 dev eth0` → ping 立即 `3/3 0% loss`。

### 2.6 SSH 连不上 → PermitRootLogin without-password

网络通了但 `ssh root@192.168.137.100` 密码被拒。

关键证据：
- `sshd -T` → `permitrootlogin without-password`（root 只许密钥登录）
- auth.log：`pam_unix(sshd:auth): authentication failure ... user=root`

**根因**：默认配置 root 只能公钥登录，密码被拒。
**修复**：写入 `/etc/ssh/sshd_config.d/99-allow-root.conf` 的 `PermitRootLogin yes`，重启 sshd。

---

## 3. 最终改动清单（已持久化到异常系统）

| 文件 | 改动 | 作用 |
|---|---|---|
| `/var/davinci/`（目录） | 从正常系统导入完整 davinci 驱动包 | 提供 hclgeplf/hns3/hnae3 等 165 个昇腾驱动 |
| `/etc/modules-load.d/10-atlas-default.conf` | 追加 `broadcom` | 开机自动加载 PHY 驱动 |
| `/etc/netplan/01-netcfg.yaml` | eth0 的 `192.168.0.2/24` → 改为 **eth1** `192.168.137.100/24` | 修正网线实际所在口的 IP，去掉冲突 IP |
| `/etc/ssh/sshd_config.d/99-allow-root.conf` | 新建，内容 `PermitRootLogin yes` | 允许 root 密码 SSH 登录 |
| （临时验证）`ip addr del 192.168.137.88/24 dev eth0` | 删除 eth0 幽灵 IP | 恢复通信（netplan 已根除该 IP 来源） |

> 附注：异常系统 root 密码已重置为 `Mind@123`（`passwd`），与正常系统一致。

---

## 4. 当前验证结果

```
$ ping 192.168.137.100
3 packets transmitted, 3 received, 0% packet loss

$ sshpass ssh root@192.168.137.100
SSH_OK / davinci-mini / 6.6.0-72.0.0.76.h914.eulerosv2r15.ascend.aarch64
eth1 UP 192.168.137.100/24
```

---

## 5. 遗留风险与建议

1. **开机时序未验证**：`broadcom` 目前靠 `/etc/modules-load.d/`（systemd 早期）加载，而 `hclgeplf` 由 minirc 脚本稍后加载。若重启后链路仍 down，需把 `broadcom` 的 insmod 挪进 `/var/davinci/scripts/minirc_sys_init.sh`（跟在 hclgeplf 之后），保证 PHY 驱动就绪时序。
2. **NPU 未验证**：davinci 驱动包已导入，`npu-smi` 是否可用、NPU 设备是否注册，建议确认（`npu-smi info` / `ls /dev/davinci*`）。
3. **系统时间漂移**：异常系统日期停留在 2022 年（无 RTC 同步），认证日志时间不可靠，建议 `timedatectl` 或 NTP 校准。

---

## 6. 相关文件（本目录）

| 文件 | 说明 |
|---|---|
| `davinci.tar.gz` | 从正常系统打包的 `/var/davinci/`（165 个 .ko） |
| `atlas200dk-fdt-5.10.dtb` / `normal.dts` | 正常系统设备树 |
| `abnormal.dtb` / `abnormal.dts` | 异常系统设备树 |
| `normal.txt` / `error.txt` | 两套系统启动日志 |
